// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AshtakavargaData, AshtakavargaErrors, AshtakavargaResponses, AshtottariDashaData, AshtottariDashaErrors, AshtottariDashaResponses, AtmakarakaData, AtmakarakaErrors, AtmakarakaResponses, BusinessNameData, BusinessNameErrors, BusinessNamePostData, BusinessNamePostErrors, BusinessNamePostResponses, BusinessNameResponses, ChaldeanData, ChaldeanErrors, ChaldeanResponses, CharDashaData, CharDashaErrors, CharDashaResponses, ChartSvgData, ChartSvgErrors, ChartSvgResponses, DashaData, DashaErrors, DashaResponses, DashaTransitsData, DashaTransitsErrors, DashaTransitsResponses, DivisionalChartsData, DivisionalChartsErrors, DivisionalChartsResponses, DoshasData, DoshasErrors, DoshasResponses, GemstonesData, GemstonesErrors, GemstonesResponses, GeocodeData, GeocodeErrors, GeocodeResponses, GocharData, GocharErrors, GocharResponses, HoroscopeDailyData, HoroscopeDailyErrors, HoroscopeDailyResponses, HoroscopeMonthlyData, HoroscopeMonthlyErrors, HoroscopeMonthlyResponses, HoroscopeWeeklyData, HoroscopeWeeklyErrors, HoroscopeWeeklyResponses, HoroscopeYearlyData, HoroscopeYearlyErrors, HoroscopeYearlyResponses, IshtaDevataData, IshtaDevataErrors, IshtaDevataResponses, KpChartData, KpChartErrors, KpChartResponses, KpRulingPlanetsData, KpRulingPlanetsErrors, KpRulingPlanetsResponses, KpSignificatorsData, KpSignificatorsErrors, KpSignificatorsResponses, LalKitabChartData, LalKitabChartErrors, LalKitabChartResponses, LalKitabRemediesData, LalKitabRemediesErrors, LalKitabRemediesResponses, LifePathData, LifePathErrors, LifePathResponses, LoShuData, LoShuErrors, LoShuResponses, LuckyNumbersData, LuckyNumbersErrors, LuckyNumbersResponses, MatchmakingDashakootData, MatchmakingDashakootErrors, MatchmakingDashakootResponses, MatchmakingData, MatchmakingErrors, MatchmakingPapasamyamData, MatchmakingPapasamyamErrors, MatchmakingPapasamyamResponses, MatchmakingPoruthamData, MatchmakingPoruthamErrors, MatchmakingPoruthamResponses, MatchmakingResponses, MatchmakingThirumanaPoruthamData, MatchmakingThirumanaPoruthamErrors, MatchmakingThirumanaPoruthamResponses, MobileNumberData, MobileNumberErrors, MobileNumberResponses, MuhurtaData, MuhurtaErrors, MuhurtaResponses, NakshatraData2, NakshatraErrors, NakshatraResponses, NameCorrectionData, NameCorrectionErrors, NameCorrectionResponses, NatalChartData, NatalChartErrors, NatalChartResponses, NumberMeaningData, NumberMeaningErrors, NumberMeaningResponses, NumerologyCompatibilityData, NumerologyCompatibilityErrors, NumerologyCompatibilityResponses, NumerologyProfileData, NumerologyProfileErrors, NumerologyProfileResponses, PanchangaCalendarData, PanchangaCalendarErrors, PanchangaCalendarPostData, PanchangaCalendarPostErrors, PanchangaCalendarPostResponses, PanchangaCalendarResponses, PanchangaChoghadiyaData, PanchangaChoghadiyaErrors, PanchangaChoghadiyaResponses, PanchangaData, PanchangaErrors, PanchangaHoraData, PanchangaHoraErrors, PanchangaHoraResponses, PanchangaRahuKaalData, PanchangaRahuKaalErrors, PanchangaRahuKaalResponses, PanchangaResponses, PersonalYearData, PersonalYearErrors, PersonalYearPostData, PersonalYearPostErrors, PersonalYearPostResponses, PersonalYearResponses, PrashnaData, PrashnaErrors, PrashnaResponses, RemediesData, RemediesErrors, RemediesResponses, SadeSatiData, SadeSatiErrors, SadeSatiResponses, StrengthData, StrengthErrors, StrengthResponses, TimezoneData, TimezoneErrors, TimezoneResponses, TransitsData, TransitsErrors, TransitsResponses, VarshaphalData, VarshaphalErrors, VarshaphalResponses, VehicleNumberData, VehicleNumberErrors, VehicleNumberResponses, YogasData, YogasErrors, YogasResponses, YoginiDashaData, YoginiDashaErrors, YoginiDashaResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Jaimini Charakarakas
 *
 * Computes all 8 Jaimini Charakarakas from the natal chart: Atmakaraka, Amatyakaraka, Bhratrikaraka, Matrikaraka, Pitrikaraka, Putrakaraka, Gnatikaraka, and Darakaraka. The planet with the highest degree in its sign becomes the Atmakaraka. Request JSON follows BirthInput: `name` (optional, default Chart), `date` (YYYY-MM-DD), `time` (HH:MM, required), either `location` or `latitude`/`longitude`/`timezone`, optional `ayanamsa` (lahiri|raman|kp|tropical).
 */
export const atmakaraka = <ThrowOnError extends boolean = false>(options: Options<AtmakarakaData, ThrowOnError>) => (options.client ?? client).post<AtmakarakaResponses, AtmakarakaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/atmakaraka',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ashtakavarga
 *
 * Computes the Ashtakavarga system for a given birth chart.
 *
 * **What is Ashtakavarga?**
 * Ashtakavarga is a Vedic system that assigns benefic points (bindus) to each
 * house based on the positions of the 7 classical planets and the Lagna (ascendant).
 * It is used to assess the relative strength of each house and to time events
 * through transit analysis — a planet transiting a house with more bindus produces
 * better results.
 *
 * **What this endpoint returns:**
 *
 * - **bhinna** — Bhinna Ashtakavarga (BAV): the raw contribution table for each
 * planet and the Lagna. Each entry maps Sanskrit sign name (Mesha…Meena) to bindus
 * in that sign.
 *
 * - **bhinna_after_trikona** — BAV after Trikona Shodana (triangular reduction).
 * Points in trinal houses are equalised. Per-planet objects keyed by sign name.
 *
 * - **bhinna_after_ekadhipatya** — BAV after both Trikona and Ekadhipatya Shodana
 * (lordship reduction). The most refined per-planet table, keyed by sign name.
 *
 * - **sarva** — Sarva Ashtakavarga (SAV): the raw sum across all 7 planets.
 * Object keyed by sign name; 28+ bindus in a sign is considered strong.
 *
 * - **sarva_reduced** — SAV computed from the fully reduced planet tables.
 * More accurate for transit timing. Keyed by sign name.
 *
 * - **after_trikona** / **after_ekadhipatya** — SAV-level reductions for
 * backward compatibility, keyed by sign name.
 *
 * **How to use bindus for transit timing:**
 * When a planet transits a house in its own Bhinna chart, the number of bindus
 * in that house indicates how favourable the transit will be. 8 bindus is maximum,
 * 0 is minimum. In the SAV, 30+ bindus in a house indicates strong positive results
 * during transits through that house.
 *
 * **Ayanamsa:** All positions are sidereal. Default is Lahiri.
 *
 * Returns Bhinna Ashtakavarga (BAV) and Sarva Ashtakavarga (SAV) scores for all planets and houses.
 */
export const ashtakavarga = <ThrowOnError extends boolean = false>(options: Options<AshtakavargaData, ThrowOnError>) => (options.client ?? client).post<AshtakavargaResponses, AshtakavargaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/ashtakavarga',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ashtottari Dasha
 *
 * Computes Ashtottari Dasha periods for a given birth chart.
 *
 * **What is Ashtottari Dasha?**
 * Ashtottari Dasha is a 108-year dasha cycle using 8 planets (excluding Ketu).
 * It is considered applicable only for specific charts where Rahu occupies a
 * Kendra (houses 1, 4, 7, 10) or Trikona (houses 1, 5, 9) from the Lagna.
 *
 * **Applicability check:**
 * This endpoint first checks whether Ashtottari applies to the given chart.
 * If Rahu is not in a Kendra or Trikona house, the response will contain
 * `applicable: false` and a reason string instead of dasha periods.
 *
 * **The 8 planets and their durations:**
 * - Sun — 6 years
 * - Moon — 15 years
 * - Mars — 8 years
 * - Mercury — 17 years
 * - Saturn — 10 years
 * - Jupiter — 19 years
 * - Rahu — 12 years
 * - Venus — 21 years
 *
 * **Levels:**
 * - `levels: 1` — returns Maha Dasha periods only
 * - `levels: 2` — returns Maha Dasha with Antar Dasha sub-periods (default)
 *
 * **Date format:** All dates are returned in DD/MM/YYYY format.
 *
 * **Ayanamsa:** Default is Lahiri.
 */
export const ashtottariDasha = <ThrowOnError extends boolean = false>(options: Options<AshtottariDashaData, ThrowOnError>) => (options.client ?? client).post<AshtottariDashaResponses, AshtottariDashaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/ashtottari',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate Kundali chart SVG
 *
 * Generate Vedic astrological chart SVGs. Returns North Indian and South Indian style charts for both Lagna (D1) and Navamsa (D9), as SVG strings. Each planet shows abbreviation, degree°minutes', and superscripts for Vakri (ᴿ), Asta (ᶜ), and Vargottama (ᵛ). Returns North Indian (Uttar Bharatiya) and South Indian (Dakshina Bharatiya) Kundali charts as inline SVG. Also returns Navamsa (D9) chart. The response contains SVG strings for each chart style. To render them:
 *
 * 1. In browser: create an HTML file,
 * paste the SVG string directly
 * into the body tag.
 *
 * 2. In React/Vue/Angular: use
 * dangerouslySetInnerHTML or v-html
 * to render the SVG string.
 *
 * 3. To preview instantly: copy the
 * lagna_north value and paste into
 * https://svgviewer.dev
 *
 * Chart styles returned:
 * - lagna_north: North Indian style
 * (diamond layout)
 * - lagna_south: South Indian style
 * (square grid layout)
 * - navamsa_north: D9 Navamsa chart
 * North Indian style
 * - navamsa_south: D9 Navamsa chart
 * South Indian style
 * - legend: R=Vakri(Retrograde)
 * C=Asta(Combust) V=Vargottama
 */
export const chartSvg = <ThrowOnError extends boolean = false>(options: Options<ChartSvgData, ThrowOnError>) => (options.client ?? client).post<ChartSvgResponses, ChartSvgErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/chart',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Natal Chart — Janam Kundali
 *
 * Calculate a complete Vedic natal chart using Swiss Ephemeris. Returns planet positions in sidereal zodiac, house placements (whole sign), ascendant, nakshatra data, Avakahada Chakra table, and optional Phaladeepika-sourced interpretation for each planet placement. Also known as Janam Kundali or Janma Kundali. Returns planets (Grahas), houses (Bhavas), ascendant (Lagna), nakshatra positions, and avakahada table.
 */
export const natalChart = <ThrowOnError extends boolean = false>(options: Options<NatalChartData, ThrowOnError>) => (options.client ?? client).post<NatalChartResponses, NatalChartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/natal',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Daily Panchanga
 *
 * Calculate Vedic Panchanga for a given date and location. Returns Tithi, Nakshatra, Yoga, Karana, Vara (weekday), and auspicious and inauspicious periods for the day. Returns Tithi, Vara (weekday), Nakshatra, Yoga, and Karana for the birth date and location.
 */
export const panchanga = <ThrowOnError extends boolean = false>(options: Options<PanchangaData, ThrowOnError>) => (options.client ?? client).post<PanchangaResponses, PanchangaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/panchanga',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Choghadiya periods
 *
 * Calculate all 16 Choghadiya periods (8 day + 8 night) for a given date and location. Each period is classified (e.g. highly auspicious for Amrit, good, beneficial, neutral, or inauspicious) with the ruling planet.
 */
export const panchangaChoghadiya = <ThrowOnError extends boolean = false>(options: Options<PanchangaChoghadiyaData, ThrowOnError>) => (options.client ?? client).post<PanchangaChoghadiyaResponses, PanchangaChoghadiyaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/panchanga/choghadiya',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Planetary hours (Hora)
 *
 * Calculate all 24 Hora (planetary hours) for a given date and location, starting from sunrise. Each hora is ruled by a planet in the Chaldean sequence with qualities indicating suitable activities.
 */
export const panchangaHora = <ThrowOnError extends boolean = false>(options: Options<PanchangaHoraData, ThrowOnError>) => (options.client ?? client).post<PanchangaHoraResponses, PanchangaHoraErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/panchanga/hora',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rahu Kaal, Gulika Kaal, Yamaganda Kaal
 *
 * Calculate the three inauspicious time periods for a given date and location. Rahu Kaal, Gulika Kaal (Mandi), and Yamaganda Kaal are avoided for starting auspicious activities. All three are based on dividing the day into 8 equal parts from sunrise to sunset.
 */
export const panchangaRahuKaal = <ThrowOnError extends boolean = false>(options: Options<PanchangaRahuKaalData, ThrowOnError>) => (options.client ?? client).post<PanchangaRahuKaalResponses, PanchangaRahuKaalErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/panchanga/rahu-kaal',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Monthly Panchanga calendar
 *
 * Calculate complete Panchanga for every day of a given month at a given location. Returns all 5 Panchanga elements (Tithi, Vara, Nakshatra, Yoga, Karana) plus Rahu Kaal for each day. Useful for building monthly almanac views.
 */
export const panchangaCalendar = <ThrowOnError extends boolean = false>(options: Options<PanchangaCalendarData, ThrowOnError>) => (options.client ?? client).get<PanchangaCalendarResponses, PanchangaCalendarErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/panchanga/calendar',
    ...options
});

/**
 * Monthly Panchanga calendar (JSON body)
 *
 * Same data as GET /v1/astro/panchanga/calendar. Minimum required JSON fields: `year` (1900–2100), `month` (1–12), `lat`, `lon`. Optional `timezone` defaults to Asia/Kolkata.
 */
export const panchangaCalendarPost = <ThrowOnError extends boolean = false>(options: Options<PanchangaCalendarPostData, ThrowOnError>) => (options.client ?? client).post<PanchangaCalendarPostResponses, PanchangaCalendarPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/panchanga/calendar',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Divisional charts — Varga
 *
 * Computes all 16 Vedic divisional charts (Varga charts) for a given
 * birth date, time, and location.
 *
 * **What are divisional charts?**
 * In Vedic astrology, the birth chart (D1) is divided into progressively finer
 * subdivisions to reveal different areas of life. Each divisional chart isolates
 * a specific domain — the D9 Navamsha reveals spouse and dharma, the D10 Dashamsha
 * reveals career, the D60 Shashtyamsha is the most subtle and reveals karmic residue
 * from past lives.
 *
 * **The 16 charts returned:**
 * - D1 — Rashi (natal chart)
 * - D2 — Hora (wealth)
 * - D3 — Drekkana (siblings)
 * - D4 — Chaturthamsha (property)
 * - D7 — Saptamsha (children)
 * - D9 — Navamsha (spouse, dharma) — the most consulted divisional chart
 * - D10 — Dashamsha (career)
 * - D12 — Dwadashamsha (parents)
 * - D16 — Shodashamsha (vehicles)
 * - D20 — Vimshamsha (spirituality)
 * - D24 — Chaturvimshamsha (education)
 * - D27 — Bhamsha (vitality)
 * - D30 — Trimshamsha (misfortunes)
 * - D40 — Khavedamsha (auspicious effects)
 * - D45 — Akshavedamsha (all life matters)
 * - D60 — Shashtyamsha (subtle karmic residue)
 *
 * Each chart shows every planet's sign placement within that divisional
 * subdivision. Use the D9 alongside the D1 for most interpretive work.
 *
 * **Ayanamsa:** All positions are sidereal. Default is Lahiri.
 *
 * Also known as Varga charts or Amsha charts. Returns D1 through D60 divisional charts.
 */
export const divisionalCharts = <ThrowOnError extends boolean = false>(options: Options<DivisionalChartsData, ThrowOnError>) => (options.client ?? client).post<DivisionalChartsResponses, DivisionalChartsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/divisional',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Daily horoscope
 *
 * Returns today's pre-generated daily horoscope for the given Moon sign. Content is computed from Vedic transit data and generated using AI. Returns 404 if today's horoscope has not been generated yet.
 */
export const horoscopeDaily = <ThrowOnError extends boolean = false>(options: Options<HoroscopeDailyData, ThrowOnError>) => (options.client ?? client).get<HoroscopeDailyResponses, HoroscopeDailyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/horoscope/daily/{moon_sign}',
    ...options
});

/**
 * Weekly horoscope
 *
 * Returns this week's pre-generated weekly horoscope for the given Moon sign.
 */
export const horoscopeWeekly = <ThrowOnError extends boolean = false>(options: Options<HoroscopeWeeklyData, ThrowOnError>) => (options.client ?? client).get<HoroscopeWeeklyResponses, HoroscopeWeeklyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/horoscope/weekly/{moon_sign}',
    ...options
});

/**
 * Monthly horoscope
 *
 * Returns this month's pre-generated monthly horoscope for the given Moon sign.
 */
export const horoscopeMonthly = <ThrowOnError extends boolean = false>(options: Options<HoroscopeMonthlyData, ThrowOnError>) => (options.client ?? client).get<HoroscopeMonthlyResponses, HoroscopeMonthlyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/horoscope/monthly/{moon_sign}',
    ...options
});

/**
 * Yearly horoscope
 *
 * Returns this year's pre-generated yearly horoscope for the given Moon sign.
 */
export const horoscopeYearly = <ThrowOnError extends boolean = false>(options: Options<HoroscopeYearlyData, ThrowOnError>) => (options.client ?? client).get<HoroscopeYearlyResponses, HoroscopeYearlyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/horoscope/yearly/{moon_sign}',
    ...options
});

/**
 * Muhurta — Auspicious Timing
 *
 * Finds and ranks auspicious time windows for a specific event type. Scores each Choghadiya period using Choghadiya quality, Yoga, Vara (weekday), and Tithi. Returns top N ranked windows. Supported events: marriage, travel, business, griha_pravesh, naming_ceremony.
 */
export const muhurta = <ThrowOnError extends boolean = false>(options: Options<MuhurtaData, ThrowOnError>) => (options.client ?? client).post<MuhurtaResponses, MuhurtaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/muhurta',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Vimshottari Dasha periods
 *
 * Calculate complete Vimshottari Dasha timeline for a birth chart. Returns all Mahadasha and Antardasha periods with dates, plus classical interpretation text for the currently active Mahadasha and Antardasha from Phaladeepika sources. Returns Mahadasha, Antardasha, Pratyantar, Sookshma, and Prana dasha periods up to 5 levels deep. Starting planet determined by Moon nakshatra (Janma Nakshatra).
 */
export const dasha = <ThrowOnError extends boolean = false>(options: Options<DashaData, ThrowOnError>) => (options.client ?? client).post<DashaResponses, DashaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/dasha',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Dasha-Transit Correlation
 *
 * Correlates active Vimshottari Dasha lords (maha, antar, pratyantar) with current planetary transits. Returns conjunction and aspect correlations scored by strength, and highlights periods of significance. Request JSON follows BirthInput plus optional `target_date`, `target_time`, `target_timezone` for the analysis moment: `name`, `date`, `time`, `location` or `latitude`/`longitude`/`timezone`, `ayanamsa`.
 */
export const dashaTransits = <ThrowOnError extends boolean = false>(options: Options<DashaTransitsData, ThrowOnError>) => (options.client ?? client).post<DashaTransitsResponses, DashaTransitsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/dasha-transits',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Jaimini Char Dasha
 *
 * Computes Jaimini Char Dasha — a sign-based dasha system from Jaimini Sutras. Returns mahadasha and antardasha periods starting from the Atmakaraka's sign, with the current active mahadasha and antardasha highlighted. Request JSON follows BirthInput plus `cycles` (1–3): `name`, `date` (YYYY-MM-DD), `time` (HH:MM, required), either `location` or `latitude`/`longitude`/`timezone`, `ayanamsa`.
 */
export const charDasha = <ThrowOnError extends boolean = false>(options: Options<CharDashaData, ThrowOnError>) => (options.client ?? client).post<CharDashaResponses, CharDashaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/char-dasha',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Dashakoot — 10-Koota Compatibility
 *
 * Computes all 10 kootas for two persons: the standard 8 Ashtakoot kootas plus Mahendra and Stree Deergha. Returns total score out of 38, per-koota breakdown, dosha analysis (Nadi, Bhakoot, Rajju, Vedha), and compatibility level.
 */
export const matchmakingDashakoot = <ThrowOnError extends boolean = false>(options: Options<MatchmakingDashakootData, ThrowOnError>) => (options.client ?? client).post<MatchmakingDashakootResponses, MatchmakingDashakootErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/matchmaking/dashakoot',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Papasamyam — Malefic Planet Compatibility
 *
 * Computes Papasamyam between two natal charts. Scores each person based on Sun, Mars, Saturn, Rahu, Ketu in houses 1, 2, 4, 7, 8, 12 from Lagna, Moon, and Venus (#105). Compatible if score difference ≤ 11. person1 = groom, person2 = bride.
 */
export const matchmakingPapasamyam = <ThrowOnError extends boolean = false>(options: Options<MatchmakingPapasamyamData, ThrowOnError>) => (options.client ?? client).post<MatchmakingPapasamyamResponses, MatchmakingPapasamyamErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/matchmaking/papasamyam',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Kerala Porutham — 10-Porutham Compatibility
 *
 * Computes all 10 Kerala Poruthams: Dinam, Ganam, Mahendra, Stree Deergha, Yoni, Rasi, Rasiyathipaty, Rajju, Vedha, and Vasya. Rajju and Vedha are absolute vetoes — if either fails the match is prohibited. person1 = groom, person2 = bride.
 */
export const matchmakingPorutham = <ThrowOnError extends boolean = false>(options: Options<MatchmakingPoruthamData, ThrowOnError>) => (options.client ?? client).post<MatchmakingPoruthamResponses, MatchmakingPoruthamErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/matchmaking/porutham',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Tamil Thirumana Porutham — 10-Porutham Compatibility
 *
 * Computes all 10 Tamil Thirumana Poruthams with Tamil naming convention. Includes Rajju severity classification (Siro=most severe to Pada=least severe). Rajju and Vedha are absolute vetoes. person1 = groom, person2 = bride.
 */
export const matchmakingThirumanaPorutham = <ThrowOnError extends boolean = false>(options: Options<MatchmakingThirumanaPoruthamData, ThrowOnError>) => (options.client ?? client).post<MatchmakingThirumanaPoruthamResponses, MatchmakingThirumanaPoruthamErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/matchmaking/thirumana-porutham',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Calculate dosha report
 *
 * Detect all major Vedic doshas in a natal chart — Mangal Dosha, Kaal Sarp Dosha, Gandmool, Grahan, Guru Chandal, Kemdrum, Shrapit, and Pitru Dosha. Present doshas include cancellation analysis, classical interpretation, keywords, and traditional remedies.
 */
export const doshas = <ThrowOnError extends boolean = false>(options: Options<DoshasData, ThrowOnError>) => (options.client ?? client).post<DoshasResponses, DoshasErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/dosha',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Gochar — Transit Analysis
 *
 * Computes planetary transits against a natal chart using classical Vedic rules. Returns house positions from Moon and Lagna, Vedha obstruction checks, Ashtakavarga Bhinna scores, Sade Sati and Chandra Ashtama flags, and Phaladeepika interpretations for all 9 planets. Request JSON follows BirthInput plus optional transit fields `target_date`, `target_time`, `target_timezone`: `name`, `date`, `time`, `location` or `latitude`/`longitude`/`timezone`, `ayanamsa`.
 */
export const gochar = <ThrowOnError extends boolean = false>(options: Options<GocharData, ThrowOnError>) => (options.client ?? client).post<GocharResponses, GocharErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/gochar',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Calculate yoga combinations
 *
 * Detect all classical Vedic yogas in a natal chart — Pancha Mahapurusha Yogas, Raja Yogas, Gajakesari, Neecha Bhanga Raja Yoga, Budhaditya, Chandra Mangala, and more. Each detected yoga returns formation conditions, classical results, modern summary, and keywords.
 */
export const yogas = <ThrowOnError extends boolean = false>(options: Options<YogasData, ThrowOnError>) => (options.client ?? client).post<YogasResponses, YogasErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/yoga',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Kundali Milan — Matchmaking
 *
 * Complete Vedic compatibility analysis using Ashtakoot Guna Milan (36-point system). Returns all 8 koota scores, Mangal Dosha compatibility with Dosha Samya analysis, classical veto checks (Vedha and Rajju Dosha), supplementary checks (Mahendra, Stree Deergha), and compatibility narrative with strengths, concerns, and recommendation. Also known as Kundali Milan, Gun Milan, or Ashtakoot Milan. Calculates 36-point compatibility score with Rajju, Vedha, and Dosha Samya classical rules.
 */
export const matchmaking = <ThrowOnError extends boolean = false>(options: Options<MatchmakingData, ThrowOnError>) => (options.client ?? client).post<MatchmakingResponses, MatchmakingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/matchmaking',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate numerology profile
 *
 * Builds a complete numerology profile from name and birth date (BirthInput-aligned: `name`, `date`). Requires authenticated API key access (Core tier or above in product terms). Returns core numbers, interpretations, cycles, and supportive metadata.
 */
export const numerologyProfile = <ThrowOnError extends boolean = false>(options: Options<NumerologyProfileData, ThrowOnError>) => (options.client ?? client).post<NumerologyProfileResponses, NumerologyProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/profile',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Calculate numerology compatibility
 *
 * Computes compatibility between two people from their numerology signatures. Each person uses `name` and `date` (BirthInput-aligned). Requires authenticated API key access (Core tier or above in product terms). Returns score, level, and explanatory strengths/challenges.
 */
export const numerologyCompatibility = <ThrowOnError extends boolean = false>(options: Options<NumerologyCompatibilityData, ThrowOnError>) => (options.client ?? client).post<NumerologyCompatibilityResponses, NumerologyCompatibilityErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/compatibility',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get life path number
 *
 * Calculates the life path number for a birth date and returns interpretation details. Query: `date` (YYYY-MM-DD). Requires authenticated API key access (Core tier or above in product terms). Returns number, master/karmic markers, and guidance keywords.
 */
export const lifePath = <ThrowOnError extends boolean = false>(options: Options<LifePathData, ThrowOnError>) => (options.client ?? client).get<LifePathResponses, LifePathErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/life-path',
    ...options
});

/**
 * Get personal year number
 *
 * Calculates personal year influence for a given birth date and target year. Query: `date`, `year`. Requires authenticated API key access (Core tier or above in product terms). Returns theme, opportunities, challenges, and practical advice.
 */
export const personalYear = <ThrowOnError extends boolean = false>(options: Options<PersonalYearData, ThrowOnError>) => (options.client ?? client).get<PersonalYearResponses, PersonalYearErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/personal-year',
    ...options
});

/**
 * Get personal year number (JSON body)
 *
 * Same calculation as GET /v1/numerology/personal-year with an MCP-friendly JSON body: `name`, `date` (YYYY-MM-DD), optional `year` (defaults to the current calendar year).
 */
export const personalYearPost = <ThrowOnError extends boolean = false>(options: Options<PersonalYearPostData, ThrowOnError>) => (options.client ?? client).post<PersonalYearPostResponses, PersonalYearPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/personal-year',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate lucky numbers
 *
 * Generates a deterministic lucky number set from `name` and `date` query params (BirthInput-aligned). Requires authenticated API key access (Core tier or above in product terms). Returns lucky numbers with supporting numerology context.
 */
export const luckyNumbers = <ThrowOnError extends boolean = false>(options: Options<LuckyNumbersData, ThrowOnError>) => (options.client ?? client).get<LuckyNumbersResponses, LuckyNumbersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/lucky-numbers',
    ...options
});

/**
 * Get number meaning by context
 *
 * Returns interpretation details for a specific number within a numerology context. Query `context` defaults to `general` (same meanings as life path). Requires authenticated API key access (Core tier or above in product terms). Returns meaning text and optional thematic guidance fields.
 */
export const numberMeaning = <ThrowOnError extends boolean = false>(options: Options<NumberMeaningData, ThrowOnError>) => (options.client ?? client).get<NumberMeaningResponses, NumberMeaningErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/meaning/{number}',
    ...options
});

/**
 * Name Correction Analysis
 *
 * Analyses a full name using Pythagorean numerology and suggests spelling variants that are more harmonious with the life path number. Returns current name scores and up to 8 alternatives ranked by harmony.
 */
export const nameCorrection = <ThrowOnError extends boolean = false>(options: Options<NameCorrectionData, ThrowOnError>) => (options.client ?? client).post<NameCorrectionResponses, NameCorrectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/name-correction',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Business Name Numerology
 *
 * Scores a business name using Pythagorean expression number. Returns single digit, theme, harmony score (1-10), and recommended uses.
 */
export const businessName = <ThrowOnError extends boolean = false>(options: Options<BusinessNameData, ThrowOnError>) => (options.client ?? client).get<BusinessNameResponses, BusinessNameErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/business-name',
    ...options
});

/**
 * Business Name Numerology (JSON body)
 *
 * Same analysis as GET /v1/numerology/business-name for clients that send a JSON body (e.g. MCP): `{ "name": "..." }`.
 */
export const businessNamePost = <ThrowOnError extends boolean = false>(options: Options<BusinessNamePostData, ThrowOnError>) => (options.client ?? client).post<BusinessNamePostResponses, BusinessNamePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/business-name',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Chaldean Numerology Profile
 *
 * Computes a full Chaldean numerology profile. Chaldean uses a different letter-value table than Pythagorean (numbers 1-8 only; 9 is sacred). Returns name number, birth number, and compound destiny number with interpretations.
 */
export const chaldean = <ThrowOnError extends boolean = false>(options: Options<ChaldeanData, ThrowOnError>) => (options.client ?? client).post<ChaldeanResponses, ChaldeanErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/chaldean',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Lo Shu Grid
 *
 * Builds a Lo Shu 3x3 numerology grid from a birth date. Returns the grid with digit counts, present/missing/repeated numbers, plane analysis (thought/will/action), and per-number trait interpretation.
 */
export const loShu = <ThrowOnError extends boolean = false>(options: Options<LoShuData, ThrowOnError>) => (options.client ?? client).post<LoShuResponses, LoShuErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/lo-shu',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Mobile Number Numerology
 *
 * Analyses a mobile/phone number numerologically. Sums all digits to a single number and returns theme, harmony score, and recommended uses.
 */
export const mobileNumber = <ThrowOnError extends boolean = false>(options: Options<MobileNumberData, ThrowOnError>) => (options.client ?? client).get<MobileNumberResponses, MobileNumberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/mobile-number',
    ...options
});

/**
 * Vehicle Number Numerology
 *
 * Analyses a vehicle registration number numerologically. Extracts digits, sums to single number, returns theme and harmony score.
 */
export const vehicleNumber = <ThrowOnError extends boolean = false>(options: Options<VehicleNumberData, ThrowOnError>) => (options.client ?? client).get<VehicleNumberResponses, VehicleNumberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/numerology/vehicle-number',
    ...options
});

/**
 * Personalised Remedies
 *
 * Prescribes personalised Vedic remedies based on planetary dignity in the natal chart. Prioritises debilitated planets, planets in enemy signs, and dusthana lords (6th/8th/12th house rulers). Returns mantras, gemstones, colours, fasting days, and daily actions. Request JSON follows BirthInput: `name`, `date`, `time`, `location` or `latitude`/`longitude`/`timezone`, `ayanamsa`. Plus `top_n` (1–9) to cap how many planets receive remedy rows.
 */
export const remedies = <ThrowOnError extends boolean = false>(options: Options<RemediesData, ThrowOnError>) => (options.client ?? client).post<RemediesResponses, RemediesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/remedies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Gemstone Recommendations
 *
 * Recommends gemstones based on the natal chart. Primary gemstone strengthens the Lagna lord. Secondary gemstone supports the Atmakaraka (soul planet). Returns contraindicated gemstones (debilitated, combust, dusthana lords, dual lagna/8th). Request JSON follows BirthInput: `name`, `date`, `time`, `location` or `latitude`/`longitude`/`timezone`, `ayanamsa`.
 */
export const gemstones = <ThrowOnError extends boolean = false>(options: Options<GemstonesData, ThrowOnError>) => (options.client ?? client).post<GemstonesResponses, GemstonesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/gemstones',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ishta Devta — Personal Deity
 *
 * Computes Ishta Devta (personal deity) from the 12th house lord of the Navamsa (D9) chart per Jaimini Sutras. Returns the ruling planet, associated deity, and D9 positions. Request JSON follows BirthInput: `name` (optional, default Chart), `date` (YYYY-MM-DD), `time` (HH:MM, required), either `location` or `latitude`/`longitude`/`timezone`, optional `ayanamsa` (lahiri|raman|kp|tropical).
 */
export const ishtaDevata = <ThrowOnError extends boolean = false>(options: Options<IshtaDevataData, ThrowOnError>) => (options.client ?? client).post<IshtaDevataResponses, IshtaDevataErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/ishta-devta',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * KP Natal Chart
 *
 * Computes the KP (Krishnamurti Paddhati) natal chart using Krishnamurti ayanamsa and Placidus house system. Returns planet positions with nakshatra lord and sub-lord, and all 12 house cusps with sub-lords. Request JSON follows BirthInput: `name`, `date` (YYYY-MM-DD), `time` (HH:MM, required), either `location` or `latitude`/`longitude`/`timezone`, optional `ayanamsa`.
 */
export const kpChart = <ThrowOnError extends boolean = false>(options: Options<KpChartData, ThrowOnError>) => (options.client ?? client).post<KpChartResponses, KpChartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/kp/chart',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * KP House Significators
 *
 * Computes KP house significators for all 12 houses. For each house returns: occupants, sign lord, planets in nakshatra of occupants (level 3), and planets in nakshatra of the sign lord (level 4). Request JSON follows BirthInput: `name`, `date` (YYYY-MM-DD), `time` (HH:MM, required), either `location` or `latitude`/`longitude`/`timezone`, optional `ayanamsa`.
 */
export const kpSignificators = <ThrowOnError extends boolean = false>(options: Options<KpSignificatorsData, ThrowOnError>) => (options.client ?? client).post<KpSignificatorsResponses, KpSignificatorsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/kp/significators',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * KP Ruling Planets
 *
 * Computes KP Ruling Planets at a given moment (no natal birth chart). Request body: `latitude`, `longitude`, optional `target_date`, `target_time`, `target_timezone`. Returns day lord, Moon sign/nakshatra/sub lords, ascendant sign/nakshatra/sub lords, and the combined list of ruling planets in priority order.
 */
export const kpRulingPlanets = <ThrowOnError extends boolean = false>(options: Options<KpRulingPlanetsData, ThrowOnError>) => (options.client ?? client).post<KpRulingPlanetsResponses, KpRulingPlanetsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/kp/ruling-planets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Lal Kitab Chart
 *
 * Computes a Lal Kitab chart using fixed sign=house mapping (Aries=House1, Taurus=House2, ..., Pisces=House12). Returns all 9 planets with Lal Kitab-specific flags: Pucca Ghar (permanent strong house), Kachcha Ghar (weak house), Uchcha (exaltation), and Neecha (debilitation). Source: Lal Kitab 1952, Pt. Roop Chand Joshi. Lahiri ayanamsa always used.
 */
export const lalKitabChart = <ThrowOnError extends boolean = false>(options: Options<LalKitabChartData, ThrowOnError>) => (options.client ?? client).post<LalKitabChartResponses, LalKitabChartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/lal-kitab/chart',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Lal Kitab Remedies
 *
 * Computes personalised Lal Kitab remedies from the birth chart. Returns practical remedies (donations, items to keep/bury, actions to avoid) for each planet based on its house placement. Malefic planets in Kachcha Ghar or Neecha are marked high priority. Source: Lal Kitab 1952, Pt. Roop Chand Joshi.
 */
export const lalKitabRemedies = <ThrowOnError extends boolean = false>(options: Options<LalKitabRemediesData, ThrowOnError>) => (options.client ?? client).post<LalKitabRemediesResponses, LalKitabRemediesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/lal-kitab/remedies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Prashna (Horary Chart)
 *
 * Computes a Prashna (Horary) chart for the exact moment a question is asked. Returns lagna, Moon analysis, house lord condition, occupants of the relevant house, and a classical verdict (favourable / unfavourable / mixed) with confidence level. Question category determines which house is analysed. Sources: Prashna Marga, Brihat Prashna Sara.
 */
export const prashna = <ThrowOnError extends boolean = false>(options: Options<PrashnaData, ThrowOnError>) => (options.client ?? client).post<PrashnaResponses, PrashnaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/prashna',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Nakshatra Profile
 *
 * Returns the complete classical profile for a given nakshatra: deity, ruling planet, gana, nature, body part, profession, life themes, strengths, challenges, favorable/unfavorable activities, and body map. Accepts nakshatra name in any case with spaces or underscores.
 */
export const nakshatra = <ThrowOnError extends boolean = false>(options: Options<NakshatraData2, ThrowOnError>) => (options.client ?? client).get<NakshatraResponses, NakshatraErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/nakshatra/{name}',
    ...options
});

/**
 * Geocode
 *
 * Resolve a city or place name to coordinates and timezone.
 *
 * Returns up to {limit} matches. When a query is ambiguous
 * (e.g. 'Fatehabad' exists in multiple states), multiple results
 * are returned so the developer can present a disambiguation UI
 * or use the first result.
 *
 * Examples: ?q=Mumbai, ?q=Rome Italy, ?q=New York USA, ?q=London UK
 *
 * Pass state/country for precise results:
 * ?q=Fatehabad,Haryana or ?q=Fatehabad&country=in
 */
export const geocode = <ThrowOnError extends boolean = false>(options: Options<GeocodeData, ThrowOnError>) => (options.client ?? client).get<GeocodeResponses, GeocodeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/utils/geocode',
    ...options
});

/**
 * Timezone From Coordinates
 *
 * Get IANA timezone for a coordinate pair.
 * Fully offline — no external API call.
 * Accepts ?latitude=&longitude= and/or ?lat=&lon= (any pair that supplies both coordinates).
 * Examples: ?latitude=19.055&longitude=72.869 (Mumbai), ?lat=19.055&lon=72.869
 */
export const timezone = <ThrowOnError extends boolean = false>(options?: Options<TimezoneData, ThrowOnError>) => (options?.client ?? client).get<TimezoneResponses, TimezoneErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/utils/timezone',
    ...options
});

/**
 * Sade Sati periods
 *
 * Calculate all Sade Sati periods for a natal chart. Returns rising, peak, and setting phases for each period, current active status, intensity score (0-100), intensity label, and next upcoming period. Optionally check status for a specific date using check_date. Sade Sati is Saturn's 7.5-year transit over natal Moon sign and adjacent signs. Returns all past, current, and future Sade Sati cycles with phase descriptions.
 */
export const sadeSati = <ThrowOnError extends boolean = false>(options: Options<SadeSatiData, ThrowOnError>) => (options.client ?? client).post<SadeSatiResponses, SadeSatiErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/sade-sati',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Calculate planetary strength suite
 *
 * Builds a composite strength report including shadbala, divisional charts, ashtakavarga, and karakas from birth input. Requires authenticated API key access (Vedic tier or above in product terms). Returns a unified strength payload for all supported modules.
 */
export const strength = <ThrowOnError extends boolean = false>(options: Options<StrengthData, ThrowOnError>) => (options.client ?? client).post<StrengthResponses, StrengthErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/strength',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Planetary transits — Gochar
 *
 * Scans a date range for planetary sign ingresses and retrograde/direct station events. Requires authenticated API key access (Vedic tier or above in product terms). Returns grouped transit events for supported planets. Also known as Gochar. Returns planetary sign ingresses and retrograde/direct station events for a given date range.
 */
export const transits = <ThrowOnError extends boolean = false>(options: Options<TransitsData, ThrowOnError>) => (options.client ?? client).post<TransitsResponses, TransitsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/transits',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Yogini Dasha
 *
 * Computes Yogini Dasha periods for a given birth chart.
 *
 * **What is Yogini Dasha?**
 * Yogini Dasha is a Nakshatra-based dasha system using 8 Yoginis, each ruled
 * by a planet. The total cycle is 36 years. It is considered highly accurate
 * for timing events in the near term and is widely used alongside Vimshottari.
 *
 * **The 8 Yoginis and their durations:**
 * - Mangala (Moon) — 1 year
 * - Pingala (Sun) — 2 years
 * - Dhanya (Jupiter) — 3 years
 * - Bhramari (Mars) — 4 years
 * - Bhadrika (Mercury) — 5 years
 * - Ulka (Saturn) — 6 years
 * - Siddha (Venus) — 7 years
 * - Sankata (Rahu) — 8 years
 *
 * **Starting Yogini** is determined by the Moon's Nakshatra at birth.
 *
 * **Levels:**
 * - `levels: 1` — returns Maha Dasha periods only
 * - `levels: 2` — returns Maha Dasha with Antar Dasha sub-periods (default)
 *
 * **Date format:** All dates are returned in DD/MM/YYYY format.
 *
 * **Ayanamsa:** Default is Lahiri.
 */
export const yoginiDasha = <ThrowOnError extends boolean = false>(options: Options<YoginiDashaData, ThrowOnError>) => (options.client ?? client).post<YoginiDashaResponses, YoginiDashaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/yogini',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Varshaphal — Solar Return Chart
 *
 * Computes the Varshaphal (annual horoscope) for a given year. Finds the exact moment when the Sun returns to its natal longitude, computes all planet positions at that moment, and returns Muntha (progressed ascendant) and Varsha Lord (year lord). Request JSON follows BirthInput plus `target_year`: `name`, `date` (YYYY-MM-DD), `time` (HH:MM, required), either `location` or `latitude`/`longitude`/`timezone`, `ayanamsa`.
 */
export const varshaphal = <ThrowOnError extends boolean = false>(options: Options<VarshaphalData, ThrowOnError>) => (options.client ?? client).post<VarshaphalResponses, VarshaphalErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v1/astro/varshaphal',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
